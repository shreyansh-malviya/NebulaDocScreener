# ---- Render free-tier (512 MB, CPU) LITE profile — onnxruntime only ----
# NO PyTorch, NO Paddle, NO system binaries → small image, fits free tier.
# Face (insightface) is intentionally EXCLUDED here to stay under 512 MB — Module 4 abstains
# gracefully when it's absent. rapidocr-onnxruntime is installed with --no-deps in the
# Dockerfile so it reuses opencv-python-HEADLESS (full opencv breaks on headless hosts).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
python-dotenv==1.0.1
cryptography==43.0.3
opencv-python-headless==4.10.0.84
pillow==11.0.0
piexif==1.1.3
numpy==2.2.6
onnxruntime==1.20.1
motor==3.6.0             # async MongoDB driver (+ pymongo, dnspython) — enables MONGODB_URI persistence
pyclipper==1.3.0.post6
shapely==2.0.6
PyYAML==6.0.3
six==1.17.0
# rapidocr-onnxruntime==1.4.4   → installed separately with --no-deps (see Dockerfile)
